version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:lts
    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            npm install
      # run tests!
      - run:
          name: run tests
          command: |
            npm run build
            npx percy snapshot docs
